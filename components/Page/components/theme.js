export const mainLogoRectangle = {
  // style object for base or default style
  baseStyle: {
    cursor: 'pointer',
    transition: '0.5s all',
    _hover: {
      opacity: 0.9
    }
  },
  // styles for different sizes ("sm", "md", "lg")
  sizes: {},
  // styles for different visual variants ("outline", "solid")
  variants: {},
  // default values for `size` and `variant`
  defaultProps: {
    size: "",
    variant: "",
  },
}